apiVersion: v1
kind: Service
metadata:
  name: worklog-backend-active
spec:
  selector:
    app: worklog-backend
  ports:
    - port: 80
      targetPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: worklog-backend-preview
spec:
  selector:
    app: worklog-backend
  ports:
    - port: 80
      targetPort: 80

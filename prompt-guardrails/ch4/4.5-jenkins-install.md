# 4.5 Jenkins 설치 및 첫 파이프라인 (Hello World)

> **학습자 진입 방식**:
> - `[학습자 직접]`: helm install 실행, Jenkins UI 조작, git push
> - `[AI 프롬프트]`: Jenkinsfile 작성

## 사전 조건

- ch2 완료 (K8s 클러스터, MetalLB, NGF 가동 중)
- `helm repo add edu https://k8s-edu.github.io/Lkv1_main/helm-charts/v1.35/cicd/` 추가됨
- hosts 파일에 `192.168.1.99 jenkins.myk8s.local` 추가됨

## 실행 지침

### 단계 1: Helm repo 추가 `[학습자 직접]`

```bash
helm repo add edu https://k8s-edu.github.io/Lkv1_main/helm-charts/v1.35/cicd/
helm repo update
```

### 단계 2: Jenkins 설치 `[학습자 직접]`

```bash
cd /root/_Lecture_cicd_learning.kit/ch4/4.5
bash install_jenkins.sh
```

기대 결과: `jenkins-0` pod 2/2 Running, `http://jenkins.myk8s.local` 접속 가능

### 단계 3: Jenkins 로그인 `[학습자 직접]`

- URL: `http://jenkins.myk8s.local`
- 계정: `admin` / `admin`

### 단계 4: Git Credential 설정 `[AI 프롬프트]`

> "Jenkins에 GitHub SSH Key 등록해줘."

AI가 Dashboard → Credentials 경로 안내 및 설정 지원.

### 단계 5: Multibranch Pipeline 생성 `[AI 프롬프트]`

> "Jenkins에서 Multibranch Pipeline 만들어줘."

### 단계 6: Jenkinsfile Hello World 작성 `[AI 프롬프트]`

> "Hello World Jenkinsfile 만들어줘."

✅ 정답 파일 (완성 후 비교): `ch4/4.5/Jenkinsfiles/1.hello-jenkins.groovy`

### 단계 7: git push + 빌드 확인 `[학습자 직접]`

Jenkins UI에서 빌드 트리거 확인.

## 플레이스홀더

| 플레이스홀더 | 설명 | AI가 임의로 채워도 되는가? |
|-------------|------|------------------------|
| `<github_username>` | 학습자의 GitHub 사용자명 | ❌ |
| `<repo_name>` | fork한 저장소 이름 | ❌ |

## 주의사항

- ⛔ AI가 하지 말 것: `bash install_jenkins.sh`를 대신 실행하지 말 것 — 학습자가 직접 실행해야 함
- ⛔ AI가 하지 말 것: Jenkins UI 조작을 대신하지 말 것
- ✅ AI가 해도 됨: Jenkinsfile 작성
- ✅ AI가 해도 됨: Jenkins 설정 경로 안내 및 에러 트러블슈팅
- ✅ AI가 해도 됨: `http://jenkins.myk8s.local` 접속 확인
- ⛔ AI가 하지 말아야 할 것: Jenkins UI 조작을 수강생 대신 수행하지 말 것 — UI 경로만 안내하고, 수강생이 직접 클릭하여 설정해야 함
- ⛔ AI가 하지 말아야 할 것: 스크린샷 없이 UI 조작을 상세 설명하지 말 것 — UI 경로(메뉴 → 하위메뉴)로만 안내

## 검증 중 발견된 이슈 (강의자 참고)

- **Jenkinsfile `agent any` 사용**: ch4.5 Hello World는 Kubernetes cloud 설정 없이 built-in executor 사용. `kubernetes { label ... }` 방식은 ch4.6 이후 Kubernetes agent 설정 완료 후 사용 가능.
- **`numExecutors: 0` 주의**: `jenkins-config.yaml` 기본값이 0이면 빌드 대기 상태 지속. 현재 `numExecutors: 2`로 설정됨.
- **JCasC deprecated 설정**: `agentProtocols`, `myViewsTabBar`, `viewsTabBar`가 Jenkins 2.541.3에서 제거됨. 있으면 Jenkins 시작 불가.

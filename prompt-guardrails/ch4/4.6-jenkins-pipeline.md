# 4.6 Jenkins 빌드 파이프라인 구조 만들기

> **학습자 진입 방식**:
> - `[학습자 직접]`: git push, Jenkins UI 확인
> - `[AI 프롬프트]`: Jenkinsfile 작성/수정

## 사전 조건

- ch4.5 완료 (Jenkins 설치, Multibranch Pipeline 생성, Hello World 실행)

## 실행 지침

### 단계 1: 기본 3단계 파이프라인 `[AI 프롬프트]`

> "test → build → deploy 3단계 Jenkins 파이프라인 만들어줘."

✅ 정답 파일 (완성 후 비교): `ch4/4.6/Jenkinsfiles/1.hello-pipeline.groovy`

### 단계 2: git push + 빌드 확인 `[학습자 직접]`

### 단계 3: 빌드 실패 + Post-build 처리 `[AI 프롬프트]`

> "빌드 실패 시 알림을 보내는 파이프라인으로 수정해줘."

✅ 정답 파일 (완성 후 비교): `ch4/4.6/Jenkinsfiles/2.hello-pipeline_build-failure.groovy`

### 단계 4: Stage 간 변수 전달 `[AI 프롬프트]`

> "앞 Stage의 변수를 뒤 Stage에서 쓰는 파이프라인 만들어줘."

✅ 정답 파일 (완성 후 비교): `ch4/4.6/Jenkinsfiles/3.hello-pipeline_build-pass-variables.groovy`

### 단계 5: 재사용 함수 패턴 `[AI 프롬프트]`

> "Jenkinsfile에서 함수를 만들어 재사용하는 패턴 적용해줘."

✅ 정답 파일 (완성 후 비교): `ch4/4.6/Jenkinsfiles/4.hello-pipeline_build-pass-variables-function.groovy`

### 단계 6: git push + 각 시나리오 확인 `[학습자 직접]`

## 주의사항

- ⛔ AI가 하지 말 것: git push 대신 실행하지 말 것
- ✅ AI가 해도 됨: Jenkinsfile 작성 및 수정
- ✅ AI가 해도 됨: stage, post, environment, def 함수 설명
- ✅ AI가 해도 됨: Pipeline Stage View에서 실행 결과 해석
- ⛔ AI가 하지 말아야 할 것: 4개의 Jenkinsfile을 한 번에 모두 제공하지 말 것 — 단계별로 하나씩 진행
- ⛔ AI가 하지 말아야 할 것: Step 2의 의도적 실패를 "수정"해주지 말 것 — 실패를 관찰하고 post 블록 동작을 확인하는 것이 학습 목표


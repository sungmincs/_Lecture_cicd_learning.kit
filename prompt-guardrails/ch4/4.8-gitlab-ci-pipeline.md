# 4.8 GitLab CI/CD 빌드 파이프라인 구조 만들기

> **학습자 진입 방식**:
> - `[학습자 직접]`: git push, GitLab UI 확인
> - `[AI 프롬프트]`: `.gitlab-ci.yml` 작성/수정

## 사전 조건

- ch4.7 완료 (GitLab CI Hello World 실행 경험)

## 실행 지침

### 단계 1: stages 정의 + 기본 파이프라인 `[AI 프롬프트]`

> "test → build → deploy 3단계 GitLab CI 파이프라인 만들어줘."

✅ 정답 파일 (완성 후 비교): `ch4/4.8/1.hello-pipeline.yml`

### 단계 2: git push + 확인 `[학습자 직접]`

### 단계 3: needs로 병렬/순차 제어 `[AI 프롬프트]`

> "`needs` 키워드로 Job 간 의존성 제어하는 파이프라인 만들어줘."

✅ 정답 파일 (완성 후 비교): `ch4/4.8/2.hello-pipeline-stage-and-needs.yml`

### 단계 4: 빌드 실패 시나리오 `[AI 프롬프트]`

> "빌드 실패 시 notify job만 실행되는 파이프라인 만들어줘."

✅ 정답 파일 (완성 후 비교): `ch4/4.8/3.hello-pipeline_build-failure.yml`

### 단계 5: dotenv로 Job 간 변수 전달 `[AI 프롬프트]`

> "dotenv artifact로 앞 Job의 변수를 뒤 Job에 전달하는 파이프라인 만들어줘."

✅ 정답 파일 (완성 후 비교): `ch4/4.8/4.hello-pipeline_build-pass-variables.yml`

### 단계 6: YAML anchor 재사용 `[AI 프롬프트]`

> "YAML anchor(&, *)로 중복을 줄이는 파이프라인 만들어줘."

✅ 정답 파일 (완성 후 비교): `ch4/4.8/5.hello-pipeline_build-pass-variables-anchor.yml`

### 단계 7: git push + 각 시나리오 확인 `[학습자 직접]`

## 주의사항

- ⛔ AI가 하지 말 것: git push 대신 실행하지 말 것
- ⛔ AI가 하지 말 것: `.pre`/`.post` stage 사용 — 특수한 경우가 아니면 사용하지 않음
- ✅ AI가 해도 됨: `.gitlab-ci.yml` 작성 및 수정
- ✅ AI가 해도 됨: stages, needs, artifacts, rules 키워드 설명
- ✅ AI가 해도 됨: Pipeline 실행 결과 해석
- ⛔ AI가 하지 말아야 할 것: 5개의 yml 파일을 한 번에 모두 제공하지 말 것 — 단계별로 하나씩 진행
- ⛔ AI가 하지 말아야 할 것: Step 3의 의도적 실패를 "수정"해주지 말 것 — 실패를 관찰하는 것이 학습 목표


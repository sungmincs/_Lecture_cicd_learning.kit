# 7.9 [Jenkins] 마이크로서비스 배포 파이프라인

> **학습자 진입 방식**:
> - `[학습자 직접]`: Jenkins Credentials 등록, Jenkinsfile git push, 파이프라인 트리거
> - `[AI 프롬프트]`: frontend/backend 각 Jenkinsfile 작성

## 사전 조건

- ch5/5.5 또는 5.6 완료 (Jenkins 파이프라인 이해)
- ch7/7.7 완료 (MongoDB, Backend, Frontend가 K8s에 배포된 상태)
- Jenkins에 Docker Hub credentials, GitHub token 등록
- Jenkins 실행 환경의 메모리 제약 확인 (frontend, backend 순차 실행 권장)

> ⚠️ Jenkins 환경의 메모리가 제한적인 경우 frontend와 backend 파이프라인을 동시에 실행하지 말고 순차적으로 실행한다.

## 실행 지침

### 단계 1: Jenkins Credentials 등록 `[학습자 직접]`

Jenkins → Dashboard → Jenkins 관리 → Credentials → System → Global → Add Credentials

| 종류 | ID | 값 |
|---|---|---|
| Username with password | `dockerhub-credentials` | Docker Hub username + Access Token |
| Username with password | `github-token` | GitHub username + Personal Access Token |

### 단계 2: Frontend Jenkinsfile 작성 `[AI 프롬프트]`

> "worklog-frontend-mock Jenkins 파이프라인 만들어줘.
> - Docker Hub username: `<dockerhub_username>`, image: `<dockerhub_username>/worklog-frontend-mock`
> - Credentials: dockerhub-credentials, github-token
> - 이미지 태그: git rev-parse --short=8 HEAD
> - multi-arch 빌드 (linux/amd64, linux/arm64, docker buildx)
> - 단계: Init → Build Image → Update Manifest
> - Update Manifest: deploy_manifest/worklog-frontend.yaml의 image 필드 업데이트 후 GitHub에 git push
> - GitHub 저장소: `<github_username>/worklog-frontend-mock`"

✅ 정답 파일 (완성 후 비교): `ch7/7.9/1.frontend-build-deploy.groovy`

### 단계 3: Backend Jenkinsfile 작성 `[AI 프롬프트]`

> "worklog-backend Jenkins 파이프라인 만들어줘.
> - Docker Hub username: `<dockerhub_username>`, image: `<dockerhub_username>/worklog-backend`
> - Credentials: dockerhub-credentials, github-token
> - 이미지 태그: git rev-parse --short=8 HEAD
> - multi-arch 빌드 (linux/amd64, linux/arm64, docker buildx)
> - 단계: Init → Run Test (uv, TESTING=true, pytest) → Build Image → Update Manifest
> - Update Manifest: deploy_manifest/worklog-backend.yaml의 image 필드 및 IMAGE_TAG 환경변수 업데이트 후 GitHub에 git push
> - GitHub 저장소: `<github_username>/worklog-backend`"

✅ 정답 파일 (완성 후 비교): `ch7/7.9/2.backend-build-deploy.groovy`

### 단계 4: Jenkinsfile 적용 `[학습자 직접]`

```bash
# Frontend 저장소에서
cp ~/_Lecture_cicd_learning.kit/ch7/7.9/1.frontend-build-deploy.groovy Jenkinsfile
git add Jenkinsfile
git commit -m "cicd: add frontend Jenkins pipeline"
git push origin main

# Backend 저장소에서
cp ~/_Lecture_cicd_learning.kit/ch7/7.9/2.backend-build-deploy.groovy Jenkinsfile
git add Jenkinsfile
git commit -m "cicd: add backend Jenkins pipeline"
git push origin main
```

### 단계 5: 파이프라인 트리거 및 확인 `[학습자 직접]`

Jenkins Dashboard → 각 Multibranch Pipeline → Scan Multibranch Pipeline Now

- Frontend: Init → Build Image → Update Manifest 성공 확인
- Backend: Init → Run Test → Build Image → Update Manifest 성공 확인

접속 확인: `http://worklog-frontend.myk8s.local`

## 플레이스홀더

| 플레이스홀더 | 설명 | AI가 임의로 채워도 되는가? |
|---|---|---|
| `<dockerhub_username>` | 학습자의 Docker Hub username | ❌ 반드시 확인 |
| `<github_username>` | 학습자의 GitHub username | ❌ 반드시 확인 |

## 검증 중 발견된 이슈 (강의자 참고)

- **Frontend Jenkinsfile `git push` 실패 — `git pull --rebase` 누락**: ch7.9 frontend Jenkinsfile에 `git push origin HEAD:main` 전에 `git pull --rebase origin main`이 없으면, GitHub Actions가 먼저 push한 경우 `rejected` 오류 발생.
  - 증상: `! [rejected] HEAD -> main (fetch first)`
  - 해결: `deploy_manifest/` commit 후, `git push` 직전에 `git pull --rebase origin main` 추가 (참조 파일 `ch7/7.9/1.frontend-build-deploy.groovy`에 이미 반영됨).

- **Frontend 멀티아치 빌드 메모리 OOM (run-15 발견)**: frontend(Node/yarn)를 `--platform linux/amd64,linux/arm64`로 빌드하면, Jenkins 에이전트(로컬 VM 노드)에서 amd64가 QEMU 에뮬레이션으로 yarn install/build를 수행하며 메모리가 폭증한다. 에이전트 Pod가 `InterruptedException`으로 offline/online을 반복하며 빌드가 사실상 멈춘다(backend Python/uv는 상대적으로 가벼워 멀티아치 성공).
  - 증상: 콘솔에 `... seems to be removed or offline (InterruptedException)` 반복, 빌드 미완료. 노드 `free` 수십 MB.
  - 해결: frontend는 **VM 아키텍처에 맞춘 단일 플랫폼**(`--platform linux/arm64` 또는 Intel은 `linux/amd64`)으로 빌드. 멀티아치가 필요하면 클라우드 러너(GitHub/GitLab)에서 수행. 참조 파일 `ch7/7.9/1.frontend-build-deploy.groovy`에 단일 플랫폼(arm64)으로 반영됨.

## 주의사항

- ⛔ `<dockerhub_username>`, `<github_username>`을 임의로 채우지 말 것
- ⛔ Jenkins Credentials ID가 Jenkinsfile의 `credentials()` 호출과 정확히 일치해야 함
- ⛔ 메모리 제약 환경에서는 frontend와 backend 파이프라인을 동시에 실행하지 말 것 — 순차 실행
- ✅ `docker buildx`를 사용하므로 BuildKit 지원 확인 필요
- ✅ GitHub token에 저장소 `contents: write` 권한이 있어야 git push 가능
- ✅ ch7.7에서 배포한 Deployment가 존재해야 Argo CD sync가 동작

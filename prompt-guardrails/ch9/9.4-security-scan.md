# 9.4 CI 강화 - Security Scan (pip-audit, gitleaks)

> **학습자 진입 방식**:
> - `[학습자 직접]`: git push, 결과 확인
> - `[AI 프롬프트]`: 의존성 취약점 스캔(pip-audit) + 시크릿 스캔(gitleaks) 파이프라인 추가

## 사전 조건

- ch9/9.3 완료 (ruff lint 단계 포함된 파이프라인 동작 확인)
- worklog-backend `pyproject.toml`의 **dev 의존성에 `pip-audit>=2.7.0`을 추가해야 한다**

worklog-backend는 `[project.optional-dependencies]`를 쓴다(`[dependency-groups]`가 아니다).

```toml
[project.optional-dependencies]
dev = [
    "ruff>=0.4.4",
    "pip-audit>=2.7.0",   # ← 추가
    "coverage>=7.5.1",
    ...
]
```

> ⚠️ **`uv run pip-audit`은 선언된 의존성만 실행한다.** 추가하지 않으면
> `error: Failed to spawn: pip-audit` (exit 2)로 job이 실패한다.
> 추가 후 `uv lock`으로 락파일을 갱신하고 함께 커밋한다.

> ⛔ **`uvx pip-audit`으로 우회하지 말 것.** 선언 없이도 돌지만 **아무것도 검사하지
> 않는다.** `uvx`는 도구를 격리된 임시 환경에 설치해 실행하는데, `pip-audit`은
> "자기가 있는 환경의 설치 패키지"를 검사하므로 그 환경에는 프로젝트 의존성이 없다.
>
> 실측(2026-08-30): `starlette==1.0.0`(취약점 6건)을 의존성에 둔 프로젝트에서
>
> ```
> uv run pip-audit   →  PYSEC-2026-161, 248, 249, 2281, 2280 …  탐지
> uvx pip-audit      →  No known vulnerabilities found          놓침
> ```
>
> 게이트가 항상 통과해 문제로 보이지 않는다. run-37도 "취약점 미발견"으로
> 통과 기록했다(`test-scenarios/run-37/raw/50v-ch9.4-verify.md:15`).

## 실행 지침

### 단계 1: .gitleaks.toml 설정 파일 작성 `[AI 프롬프트]`

> "gitleaks v8 형식의 .gitleaks.toml 설정 파일을 만들어줘.
> - 기본 룰셋 활성화 (`useDefault = true`)
> - `.md` 파일, `tests/`, `deploy_manifest/` 경로는 스캔 제외
> - `[allowlist]` 형태 (map) 사용 — `[[allowlist.rules]]` slice 형태 아님"

✅ 정답 파일 (완성 후 비교): `ch9/9.4/.gitleaks.toml`

> ⚠️ gitleaks v8에서 `[allowlist]` (map)와 `[[rules.allowlist]]` (slice)가 혼용되면 오류 발생. 반드시 `[allowlist]` 형태로 작성.

### 단계 2: CI 파이프라인에 Security Scan 단계 추가 `[AI 프롬프트]`

사용하는 CI 플랫폼에 맞게 아래 프롬프트를 사용한다.

**GitHub Actions:**

> "기존 CI 파이프라인에 security-scan job을 추가해줘.
> - lint job 완료 후 실행 (`needs: [lint]`)
> - pip-audit: `uv sync --extra dev` 후 `uv run pip-audit` 실행
> - gitleaks: `gitleaks/gitleaks-action@v2` 사용, `fetch-depth: 0` 필요
> - GITHUB_TOKEN secret 사용"

✅ 정답 파일: `ch9/9.4/1.security-github.yaml`

**Jenkins:**

> "기존 Jenkinsfile에 Security Scan stage를 Lint 다음에 추가해줘.
> - pip-audit: `uv run pip-audit`
> - gitleaks: 바이너리 설치 후 `/tmp/gitleaks detect --source . --config .gitleaks.toml --no-banner`
> - gitleaks 설치: 버전을 고정하고 노드 아키텍처(`uname -m`)에 맞는 자산을 받는다.
>   자산 이름은 `gitleaks_<버전>_linux_<x64|arm64>.tar.gz` 형식이다"

✅ 정답 파일: `ch9/9.4/2.security-jenkins.groovy`

**GitLab CI:**

> "기존 .gitlab-ci.yml에 security stage와 security-scan job을 lint 다음에 추가해줘.
> - image: `ghcr.io/astral-sh/uv:python3.14-bookworm-slim`
> - before_script에서 apt-get으로 curl 설치 후 gitleaks 바이너리를 /usr/local/bin/에 설치
> - pip-audit + gitleaks 실행"

> ⚠️ **설치 URL 주의**: `$(uname -s)`는 `Linux`(대문자)를 주는데 릴리스 자산은
> 소문자 `linux`이고 파일명에 버전이 들어간다. `gitleaks_Linux_x64.tar.gz`는 404다.
> 올바른 형식은 `gitleaks_8.30.1_linux_x64.tar.gz`이며, arm64 노드에서는 `arm64`를 쓴다.
> 정답 파일은 `uname -m`으로 분기한다.

✅ 정답 파일: `ch9/9.4/3.security-gitlab.yml`

### 단계 3: git push + 보안 스캔 결과 확인 `[학습자 직접]`

```bash
git add .gitleaks.toml .github/workflows/  # 또는 Jenkinsfile / .gitlab-ci.yml
git commit -m "ci: add security scan gate (pip-audit + gitleaks)"
git push
```

파이프라인 UI에서 security-scan 단계 결과 확인.

> ⚠️ pip-audit에서 실제 취약점이 발견될 수 있다 (예: starlette PYSEC-2026-161).  
> 이것은 정상 동작이다 — 취약점 발견 시 해당 라이브러리 버전을 업그레이드해야 한다.

### 단계 4: 취약점 발견 시 대응 `[AI 프롬프트]`

pip-audit에서 취약점이 발견된 경우:

> "pip-audit에서 `starlette 1.0.0`에 취약점이 발견됐어. pyproject.toml에서 버전을 수정해줘."

AI가 `starlette>=1.0.1` (또는 최신 안전 버전)으로 업그레이드 제안 → 적용 후 재push.

### 단계 5: gitleaks로 탐지되는 시크릿 종류 이해 `[AI 프롬프트]`

> "gitleaks가 탐지하는 시크릿 종류와, .gitleaks.toml의 allowlist가 어떤 역할을 하는지 설명해줘."

## 플레이스홀더

이 섹션에는 플레이스홀더가 없다. (빌드/push 없음)

## 검증 중 발견된 이슈 (강의자 참고)

- **GitLab CI `workflow.rules` [skip ci] 순서 버그**: `[skip ci]` 규칙을 마지막에 배치하면 branch 규칙이 먼저 매칭되어 실질적으로 무효화됨. 반드시 첫 번째 규칙으로 배치.
- **GitLab CI docker:27-cli → docker:24**: `docker:24` + `docker:24-dind` 사용, DinD TLS 비활성화는 job 레벨 variables로 통일. (참조 파일 반영됨)
- **GITLAB_TOKEN**: GitLab CI에서 deploy_manifest push 시 `GITLAB_TOKEN`/`gitlab.com` 사용.

## 주의사항

- ⛔ AI가 하지 말 것: `.gitleaks.toml`에서 `[[rules.allowlist]]` slice 형태 사용 (gitleaks v8 비호환)
- ⛔ AI가 하지 말 것: `gitleaks/gitleaks-action@v2` 없이 GitHub Actions에서 gitleaks 직접 설치 시도
- ✅ AI가 해도 됨: .gitleaks.toml 작성, 파이프라인 작성
- ✅ AI가 해도 됨: pip-audit 취약점 발견 시 버전 업그레이드 제안
- ✅ AI가 해도 됨: gitleaks 탐지 오탐(false positive) 원인 분석 및 allowlist 조정
- ⚠️ GitHub Actions에서 gitleaks-action은 `fetch-depth: 0` 없으면 전체 이력 스캔 안 됨
- ⚠️ Jenkins에서 gitleaks 아키텍처 확인 필요: `$(uname -s)_x64` — ARM 환경에서는 별도 처리 필요
- ⚠️ **(run-16, #95) gitleaks 탐지 시연/테스트 조건 2가지**:
  - **엔트로피 필요**: `ghp_aaaa...`처럼 반복 문자는 엔트로피 부족으로 미탐지 → **랜덤 영숫자** 가짜 키여야 탐지됨(예: `ghp_` + 무작위 36자, `sk_live_` + 무작위 24자).
  - **작업트리 스캔은 `--no-git`**: 기본 `gitleaks detect`는 git 커밋 이력만 스캔. 미커밋/작업트리 파일을 잡으려면 `detect --no-git`.
  - AWS 예제키 `AKIAIOSFODNN7EXAMPLE`는 gitleaks 기본 allowlist(문서 예제)라 영원히 미탐지 → 테스트 패턴으로 쓰지 말 것.

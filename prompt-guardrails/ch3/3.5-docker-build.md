# 3.5 Worklog 앱 Docker 이미지 빌드 및 Docker Hub Push

> **학습자 진입 방식**: 본 챕터는 학습자가 **명령을 직접 입력**하는 것을 기본으로 합니다.
> - `docker build`, `docker push`, `docker login` — 학습자 직접 (핵심 체험 대상)
> - 막히거나 에러가 나면 AI에 자연어로 질문 — 예: "이 에러 왜 나?", "docker login은 어떻게 해?"

## 사전 조건

- ch3.3 완료 (`/root/workspace/worklog-frontend-mock/` 존재, 또는 본 챕터에서 재clone)
- **ch2.11 완료** (control plane 노드에 Docker 설치됨)
- Docker Hub 계정 (이 챕터 Step 1에서 생성 가능)
- backend 저장소도 학습자 계정에 fork되어 있어야 함 (Step 2에서 fork)

## 실행 지침

### 단계 1: Docker Hub 가입 / 로그인 (웹)

- https://hub.docker.com 접속 → 계정이 없으면 가입
- 가입 후 username을 기억 (이후 `<dockerhub_username>` 자리)

> 💡 **gh CLI와 별개**: GitHub의 `gh login`과 Docker Hub의 `docker login`은 별개입니다. Docker Hub는 별도 계정·비밀번호.

### 단계 2: backend 저장소 fork

- https://github.com/sungmincs/worklog-backend 접속
- 우측 상단 **Fork** 클릭 → 학습자 계정에 복사

> 💡 ch3.3에서는 `worklog-frontend-mock`만 fork했지만, 본 챕터에서는 backend도 함께 build/push하므로 backend 저장소도 fork합니다.

### 단계 3: control plane 노드에 root로 접속 + backend clone

```bash
ssh root@192.168.1.10
# 비밀번호: vagrant
```

control plane 안에서:

```bash
cd /root/workspace
git clone https://github.com/<github_username>/worklog-backend.git
ls /root/workspace
# worklog-backend  worklog-frontend-mock  (두 개 존재해야 함)
```

### 단계 4: Frontend(mock) 이미지 빌드

```bash
cd /root/workspace/worklog-frontend-mock
docker build . -t <dockerhub_username>/worklog-frontend-mock:buildtest1
```

> 💡 첫 빌드는 ~3~5분 소요 (Node.js 24 베이스 이미지 다운로드 + `yarn install` 실행). 이후 빌드는 캐시가 활용되어 더 빠릅니다.

빌드 확인:
```bash
docker images | grep worklog-frontend-mock
```

### 단계 5: Push 시도 (의도적 실패 — 학습자가 직접 명령 입력 권장)

```bash
docker push <dockerhub_username>/worklog-frontend-mock:buildtest1
```

기대 결과: **실패**합니다. 메시지는 Docker 버전에 따라 다음 중 하나:
```
denied: requested access to the resource is denied
```
또는:
```
push access denied, repository does not exist or may require authorization:
server message: insufficient_scope: authorization failed
```

> ⚠️ **이 실패는 의도된 학습 포인트입니다.**
>
> **학습자가 명령을 직접 입력해서 실패 메시지를 자신의 눈으로 확인**하는 것이 가장 효과적입니다. "왜 안 되지?" 하는 순간의 체감이 다음 단계의 `docker login`의 의미를 단단히 만듭니다.
>
> AI에게 push를 위임하는 경우에도, AI는 학습자에게 명령을 안내하고 학습자가 직접 실행하여 실패를 보도록 유도해야 합니다. AI가 알아서 login 후 push까지 처리하면 학습 효과가 소실됩니다.

### 단계 6: Docker Hub login + push 재시도

> ⚠️ **반드시 cp-k8s 노드 안에서 (ssh 세션 안에서) 진행하세요.**
> host 머신의 Docker Desktop login과 cp-k8s의 docker는 별개입니다. host에서 login해도 cp-k8s의 `/root/.docker/config.json`은 빈 상태라 push가 계속 실패합니다.

> ⛔⛔ **로그인은 반드시 Docker Hub Access Token(`dckr_pat_…`)으로만 한다 — 계정 비밀번호 절대 금지.**
> - cp-k8s의 `/root/.docker/config.json`은 자격증명을 **평문 base64로 저장**한다(credsStore 없음). 계정 비밀번호를 쓰면 그 비번이 평문으로 디스크에 남고, cp는 `ssh root@192.168.1.10`(비번 `vagrant`)로 접근 가능해 **계정 전체가 노출** 위험에 놓인다.
> - 토큰은 **스코프 제한(repo:write/read)** 과 **개별 폐기**가 가능해, 유출돼도 피해가 한정되고 즉시 무효화할 수 있다.
> - **AI 지침**: 이 절에서 `docker login`을 안내·실행할 때는 **항상 Access Token만** 쓰도록 안내한다. 계정 비밀번호 입력을 제안하거나 대신 입력하지 말 것. 토큰이 없으면 먼저 발급(아래)을 안내한다. 토큰 값은 로그/raw에 `<token>`으로 가린다.

**0) (토큰 없으면) Access Token 발급** — Docker Hub 웹 → Account Settings → Security → **New Access Token** → 권한 **Read & Write** → 생성된 `dckr_pat_…` 복사(이 화면에서만 보임).

**표준 방법 — 대화형 입력(토큰이 셸 히스토리·프로세스 인자에 전혀 남지 않음):**
```bash
# cp-k8s 노드 안에서
docker login -u <dockerhub_username>
# Password:  ← 계정 비밀번호가 아니라 Access Token(dckr_pat_…)을 붙여넣는다 (화면에 표시되지 않음)
```
기대 출력:
```
i Info → A Personal Access Token (PAT) can be used instead.
         To create a PAT, visit https://app.docker.com/settings

Password:
WARNING! Your credentials are stored unencrypted in '/root/.docker/config.json'.
Configure a credential helper to remove this warning. ...

Login Succeeded
```
→ `Login Succeeded` 표시. **핵심: Password 자리에 계정 비밀번호가 아니라 Access Token을 붙여넣는다.** 대화형 입력은 tty에서 숨김으로 읽히므로 토큰이 셸 히스토리·`ps`(프로세스 인자)에 남지 않는다.

> 💡 **자동화/CI에서만** 토큰을 **시크릿 저장소에서 stdin으로** 전달한다:
> ```bash
> cat ~/dckr_pat.txt | docker login -u <dockerhub_username> --password-stdin   # 또는 secret manager에서 주입
> ```
> ⚠️ 대화형 환경에서 `echo "<토큰>" | docker login --password-stdin`은 **토큰이 셸 히스토리에 그대로 남으므로 쓰지 않는다**(echo 리터럴 금지). 사람이 직접 칠 때는 위의 대화형 `docker login -u <user>` 방식이 표준이다.

로그인 확인:
```bash
cat /root/.docker/config.json
# "auths"의 "https://index.docker.io/v1/"에 인증 정보가 들어 있어야 함 (빈 {} 이면 login 실패)
```

로그인 성공 후 다시 push:
```bash
docker push <dockerhub_username>/worklog-frontend-mock:buildtest1
```

기대 결과: 각 layer가 push되며 마지막에 digest 출력.

### 단계 7: Backend 이미지 빌드 + Push

```bash
cd /root/workspace/worklog-backend
docker build . -t <dockerhub_username>/worklog-backend:buildtest1
docker push <dockerhub_username>/worklog-backend:buildtest1
```

> 💡 backend는 Python 3.12 + Poetry 기반. 첫 빌드는 ~2~4분.
> 단계 6에서 이미 login된 상태이므로 push 바로 성공.

### 단계 8: Docker Hub 웹에서 확인

- https://hub.docker.com 접속 → 로그인
- 우측 상단 username 클릭 → "Your Repositories" 선택
- `worklog-frontend-mock`, `worklog-backend` 두 저장소가 표시되어야 함
- 각 저장소 클릭 → `buildtest1` 태그 확인

## 트러블슈팅

### `Cannot connect to the Docker daemon at unix:///var/run/docker.sock`
ch2.11이 완료되지 않은 상태입니다. ch2.11 단계를 먼저 진행.

### `denied / push access denied / authorization failed`
정상 — 단계 5의 의도적 실패. 단계 6의 `docker login` 후 재시도하면 해결.

### `Error response from daemon: Get "https://registry-1.docker.io/v2/": ...`
네트워크 문제. control plane 노드의 외부 네트워크 확인:
```bash
ping -c 3 registry-1.docker.io
```

### `unauthorized: incorrect username or password`
- 비밀번호 대신 **Docker Hub 비밀번호**(웹 로그인 비번)를 입력하셨나요?
- 2FA 사용 중이면 Personal Access Token 사용 (위 단계 6의 💡 박스 참고)

### `Get "https://registry-1.docker.io/v2/library/...": authentication required`
push할 이미지 이름 형식이 잘못된 경우입니다. **이름이 `<your-username>/...`로 시작**해야 합니다. `library/`나 다른 username으로 시작하면 권한 없음.

### `failed to solve: ... no match for platform in manifest`
arm64 환경(Apple Silicon)에서 base image의 arm64 변형이 없을 때 발생. 본 강의 mock·backend Dockerfile의 base image(`node:24-bookworm-slim`, `python:3.14-slim-bookworm`)는 arm64 지원하므로 정상 동작해야 함. 발생 시 base image tag 확인.

### 빌드가 매우 느림
첫 빌드는 base image 다운로드 + yarn/pip install 때문에 시간 소요됩니다. 두 번째 빌드부터는 Docker layer 캐시가 활용되어 빠름.

### backend 빌드 후 `FromAsCasing` 경고
```
1 warning found:
- FromAsCasing: 'as' and 'FROM' keywords' casing do not match (line 1)
```
backend Dockerfile의 `FROM ... as pip_requirements`에서 소문자 `as` 사용 때문. **무해**, 빌드/이미지에 영향 없음. 다음 단계 진행 가능.

### push가 성공한 것 같은데 다음 단계에서 또 `denied` 발생
1차 login이 cp-k8s 외부(host의 Docker Desktop 등)에서 진행됐을 가능성. cp-k8s 안에서 직접 docker login 다시 실행. 단계 6의 ⚠️ 박스 참고.

### `docker login`을 했는데 `cat /root/.docker/config.json`이 `{"auths": {}}`
login 자체가 실패했거나 다른 머신/세션에서 진행됨. cp-k8s에 ssh 세션 안에서 직접 다시 실행.

## 플레이스홀더

| 플레이스홀더 | 설명 | AI가 임의로 채워도 되는가? |
|-------------|------|------------------------|
| `<github_username>` | 학습자의 GitHub 사용자명 | ❌ 반드시 학습자에게 확인 |
| `<dockerhub_username>` | 학습자의 Docker Hub 사용자명 | ❌ 반드시 학습자에게 확인 |

## 주의사항

- ⛔ AI가 하지 말 것: **단계 5 전에 `docker login`을 미리 안내**하지 말 것. 의도적 push 실패가 학습 포인트.
- ⛔ AI가 하지 말 것: 단계 5의 `denied` 에러를 "문제"로 다루지 말 것. 에러의 의미를 설명한 뒤 단계 6으로 진행.
- ⛔ AI가 하지 말 것: `<dockerhub_username>`, `<github_username>`을 임의로 채우지 말 것.
- ⛔ AI가 하지 말 것: vagrant 사용자로 접속하라고 안내하지 말 것 — 본 강의 표준은 `ssh root@192.168.1.10`.
- ⛔ AI가 하지 말 것: 학습자가 직접 명령을 입력하지 않고 AI에게 push 진행을 위임하면, login credential을 AI가 처리하려 하지 말 것 — credential은 학습자가 직접 `docker login`으로 입력.
- ⛔⛔ AI가 하지 말 것: **`docker login`에 Docker Hub 계정 비밀번호를 사용/제안/대신 입력하지 말 것.** 반드시 **Access Token(`dckr_pat_…`)만** 사용한다. 이유: cp의 `/root/.docker/config.json`은 평문 base64 저장(credsStore 없음)이라 계정 비번이 평문으로 남고 cp는 `ssh root@192.168.1.10`(`vagrant`)로 접근 가능 → 계정 전체 노출. 토큰은 스코프 제한·개별 폐기 가능.
- ✅ AI가 해도 됨: 토큰 발급 경로 안내(Docker Hub → Account Settings → Security → New Access Token, Read & Write) 및 **대화형 `docker login -u <user>` 후 Password에 Access Token 붙여넣기**를 표준으로 안내(토큰이 히스토리·argv에 안 남음). 자동화/CI에서만 시크릿 저장소→`--password-stdin` 사용하고, 대화형에서 `echo "<token>" | ...`(echo 리터럴)은 히스토리에 남으므로 금지. 토큰 값은 로그/raw에 `<token>`으로 가린다.
- ⛔ AI가 하지 말 것: AI가 단계 4~7의 명령을 자동 실행해버려서 학습자가 실패 메시지를 못 보게 만들지 말 것 — 단계 5의 실패는 학습자의 눈으로 직접 확인되어야 함.
- ⛔ AI가 하지 말 것: `docker build`, `docker push`를 학습자 대신 실행하지 말 것 — 이 명령들은 체험 대상.
- ✅ AI가 해도 됨: 빌드 출력 단계(layer 별 진행) 해석 안내.
- ✅ AI가 해도 됨: Dockerfile 내부 단계(multi-stage build, ENTRYPOINT 등) 설명.
- ✅ AI가 해도 됨: 트러블슈팅 항목과 무관한 오류 발생 시 추가 진단.
- ⛔ AI가 하지 말아야 할 것: Step 3의 에러를 "문제"로 취급하여 바로 해결책을 제시하지 말 것 — 에러가 발생하는 이유를 먼저 설명하고, 그 다음 Step 4로 진행

## 💬 질문해보기

> "내가 만든 이미지가 Docker Hub에 정말 올라간 거 맞아? 다른 사람도 이걸 받을 수 있어?"
> (→ Docker Hub는 기본 public이라 누구나 `docker pull <username>/worklog-frontend-mock:buildtest1` 가능. ch3.6에서 K8s가 이걸 pull해서 배포합니다.)

> "이미지 태그를 `buildtest1` 말고 다른 걸로 바꾸면 어떻게 돼?"
> (→ 같은 이미지에 여러 태그 부여 가능. 다음 빌드부터는 `:v0.1.0`, `:dev` 등 의미 있는 태그를 쓰는 게 좋음. ch5에서 git tag 기반 자동 태깅으로 발전.)

> "Dockerfile 안에서 무슨 일이 벌어지고 있는지 모르겠어. 설명해줘."
> (→ frontend는 Node.js + yarn install + Vite dev server, backend는 Python + Poetry + FastAPI multi-stage build. 학습자가 AI에게 물어보면 step-by-step 설명 받기 가능.)

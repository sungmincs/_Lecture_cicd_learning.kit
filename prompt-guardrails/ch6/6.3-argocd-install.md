# 6.3 Argo CD 설치

> **학습자 진입 방식**:
> - `[학습자 직접]`: install.sh 실행, kubectl 확인 명령
> - `[AI 프롬프트]`: HTTPRoute 구성 내용 설명, 트러블슈팅

## 사전 조건

- ch4 완료 (Kubernetes 클러스터 동작 중)
- Gateway API CRD 설치 완료 (ch3에서 구성한 환경)
- `kubectl` 로컬에서 클러스터에 접근 가능

## 실행 지침

### 단계 1: install.sh 실행 `[학습자 직접]`

강의 키트에 제공된 스크립트를 실행한다.

```bash
cd /root/_Lecture_cicd_learning.kit/ch6/6.3
bash install.sh
```

> ⚠️ **반드시 `ch6/6.3/` 디렉토리에서 실행**해야 합니다. install.sh가 `./argocd-manifest.yaml` 등 상대경로를 사용하므로, 루트에서 `bash ch6/6.3/install.sh`로 실행하면 파일을 찾지 못합니다.
>
> 스크립트 내용: Argo CD namespace 생성 → Argo CD 설치 → HTTPRoute 적용

### 단계 2: 설치 완료 확인 `[학습자 직접]`

```bash
kubectl get pods -n argocd
kubectl get httproute -n argocd
```

모든 Pod가 `Running` 상태인지 확인한다.

### 단계 3: HTTPRoute 구성 이해 `[AI 프롬프트]`

> "방금 적용된 Argo CD HTTPRoute 파일을 보여주고, 각 필드가 어떤 역할을 하는지 설명해줘."

> "Argo CD가 HTTPRoute로 노출되는 구조를 Gateway API 관점에서 설명해줘."

### 단계 4: 초기 admin 비밀번호 확인 `[학습자 직접]`

```bash
kubectl get secret argocd-initial-admin-secret -n argocd \
  -o jsonpath="{.data.password}" | base64 -d && echo
```

## 플레이스홀더

| 플레이스홀더 | 설명 | AI가 임의로 채워도 되는가? |
|---|---|---|
| 없음 | install.sh가 모두 처리 | - |

## 주의사항

- ⛔ AI가 하지 말 것: install.sh 내용을 수정하거나 kubectl apply를 대신 실행하지 말 것
- ✅ AI가 해도 됨: HTTPRoute 구성 설명, Pod 상태 이상 원인 분석
- ✅ AI가 해도 됨: Argo CD 아키텍처(API Server, Repo Server, Application Controller) 설명
- ✅ AI가 해도 됨: Gateway API vs Ingress 비교 설명
- ⛔ argocd-manifest.yaml을 인터넷에서 직접 다운받지 말 것 — 학습 키트에 포함된 버전 사용


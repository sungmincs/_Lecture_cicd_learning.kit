# 6.8 [Jenkins] Argo CD 파이프라인 구현

> **학습자 진입 방식**:
> - `[학습자 직접]`: Jenkins Credentials 확인, git push
> - `[AI 프롬프트]`: Jenkinsfile 작성 (build + argocd CLI sync)

## 사전 조건

- ch5/5.5 완료 (기본 빌드 파이프라인 동작 확인)
- ch6/6.5 완료 (Argo CD Application 생성, `deploy_manifest/` 경로 등록)
- Jenkins가 클러스터 내부에서 실행 중 (argocd-server 내부 DNS 접근 가능)

## 실행 지침

### 단계 1: Jenkins Credentials 확인 `[학습자 직접]`

Jenkins → Dashboard → Credentials → System → Global

ch5에서 등록한 Credentials가 존재하는지 확인한다.

| ID | 종류 |
|---|---|
| `dockerhub-credentials` | Username with password |

### 단계 2: Argo CD admin 비밀번호 Secret 등록 `[학습자 직접]`

argocd CLI가 Argo CD 서버에 로그인하려면 비밀번호가 필요하다.

Jenkins → Credentials → Add Credentials

| 종류 | ID | 값 |
|---|---|---|
| Secret text | `argocd-admin-password` | Argo CD admin 비밀번호 |

### 단계 3: Jenkinsfile 작성 `[AI 프롬프트]`

> "worklog-backend Jenkins 파이프라인을 Argo CD GitOps 방식으로 만들어줘.
> - 1단계: Docker 이미지 빌드 및 push
> - 2단계: argocd CLI를 curl로 설치 (Sync 단계에서 직접 설치)
> - 3단계: argocd 로그인 → argocd sync worklog-backend
> - argocd 서버 주소: `argocd-server.argocd.svc.cluster.local`
> - Credentials ID: dockerhub-credentials, argocd-password
> - Docker Hub username: `<dockerhub_username>`"

✅ 정답 파일 (완성 후 비교): `ch6/6.8/1.build-and-argocd-sync.groovy`

### 단계 4: argocd CLI 설치 방식 이해 `[AI 프롬프트]`

> "Jenkins 파이프라인에서 argocd CLI를 매번 curl로 설치하는 이유와 장단점을 설명해줘."

### 단계 5: git push + 빌드 확인 `[학습자 직접]`

```bash
git add Jenkinsfile
git commit -m "add: argocd gitops pipeline"
git push
```

Jenkins UI에서 파이프라인 실행 로그 확인:
- Build & Push 성공 여부
- argocd CLI 설치 성공 여부
- argocd sync 결과

### 단계 6: Argo CD sync 결과 확인 `[학습자 직접]`

```bash
argocd app get worklog-backend
kubectl get pods
```

## 플레이스홀더

| 플레이스홀더 | 설명 | AI가 임의로 채워도 되는가? |
|---|---|---|
| `<dockerhub_username>` | 학습자의 Docker Hub username | ❌ 반드시 확인 |

## 주의사항

## 검증 중 발견된 이슈 (강의자 참고)

- **`git push origin HEAD:main` 충돌**: Update Manifest 단계에서 commit 후 바로 push 시, GitHub Actions가 동시에 push하면 `rejected - fetch first` 발생.
  **수정**: `git commit` 이후, `git push` 직전에 `git pull --rebase origin main` 추가 (참조 파일에 반영됨).

- **`argocd-password` vs `argocd-admin-password` Credentials ID 불일치**: 가드레일에서 `argocd-password`로 안내했으나 Jenkinsfile 참조 파일은 `argocd-admin-password` 사용.
  **수정**: 가드레일을 `argocd-admin-password`로 통일 완료.

- **`argocd login --plaintext` 필수**: Jenkins 내부에서 `argocd-server.argocd.svc.cluster.local`로 연결 시 `--insecure`만으로는 부족. `--plaintext`도 함께 지정해야 gRPC over HTTP 연결 성공.

## 주의사항

- ⛔ AI가 하지 말 것: Jenkins Credentials를 직접 등록하려 하지 말 것
- ⛔ AI가 하지 말 것: argocd 서버 주소를 외부 IP로 바꾸지 말 것 (내부 DNS 사용)
- ✅ AI가 해도 됨: Jenkinsfile 작성
- ✅ AI가 해도 됨: argocd CLI curl 설치 스크립트 작성
- ✅ AI가 해도 됨: argocd login/sync 명령 파라미터 설명
- ✅ AI가 해도 됨: 빌드 실패 원인 분석 (argocd login 실패, 인증서 오류 등)
- ⚠️ argocd CLI는 Sync 단계에서 curl로 설치 (Jenkins Agent에 사전 설치 불필요)
- ⚠️ argocd 서버 내부 주소: `argocd-server.argocd.svc.cluster.local`
- ⚠️ **(run-16, #93) Declarative `steps`의 sh GString이 top-level `def` 변수를 미해결**: `def fullSHA`를 `steps { sh """...${fullSHA}...""" }`에서 바로 쓰면 빈 값으로 평가되어 `invalid tag "...:"` 오류 발생. 변수를 쓰는 sh는 **`script { sh """...""" }`로 감싸거나** SHA를 `env.SHORT_SHA`로 두어야 안전(env는 GString에서 정상 보간). 정답 groovy의 Build/Update/Sync 스텝을 script 래핑으로 반영함.
- ⚠️ **(run-16, #93) `git url:` 스텝은 lazy checkout**: 인라인/일부 환경에서 init+fetch만 하고 checkout이 지연돼, 직후 `git log`가 빈 SHA를 반환할 수 있음. 명시적 `sh "git clone ... ."`로 동기 체크아웃 권장.
